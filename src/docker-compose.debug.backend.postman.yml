version: '3.8'

services:
  mongo:
    image: mongo:latest
    container_name: mongo_db
    ports:
      - "27017:27017"
    volumes:
      - mongo-dev-data:/data/db
    networks:
      - backend-network

volumes:
  mongo-dev-data:

networks: 
  backend-network: 
    driver: bridge
  
